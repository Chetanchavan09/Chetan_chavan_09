#include<stdio.h>

void main()
{
	 int year = 2004;
	 
	 if (year % 4 == 0)
	 
	 {
	 	printf("%d is leaf year \n",year);
	 }
	 
	 else
	 
	 {
	 	printf("%d is not leaf year \n",year);
	 }
	 
	 return 0;
}